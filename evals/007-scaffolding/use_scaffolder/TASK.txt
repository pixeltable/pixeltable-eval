I want to start a new Pixeltable project that provides a REST API for semantic search over documents.

Requirements:
- Scaffold the project with `uvx pixeltable-new` or `pxt init` + `pxt service example` (do NOT create the project structure by hand)
- The project name should be "doc-search"
- Install dependencies with `uv sync`
- Edit the generated app.py so it defines a documents table (TableModel) with: a path column (pxt.Document), a chunks view built with document_splitter, an embedding index declared on the model (__indexes__), and a @pxt.query function for semantic search exposed via a FastAPIRouter query route
- Show the commands to create the tables and start the service

Files to create:
- app.py (the Pixeltable TableModel + routes definition)
- The project must be scaffolded via an official path, not manually created
